# --- Secrets: never commit ---
.env
.env.local
.env.*.local
*.key
*.pem
secrets.json

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- Large / regenerable retrieved payloads ---
# Raw artifact bodies are content-addressed and can be large; the provenance
# ledger (data/raw/INDEX.jsonl) IS committed so the corpus stays auditable.
data/raw/blobs/
*.nc
*.hdf
*.h5

# --- OS ---
.DS_Store
Thumbs.db
